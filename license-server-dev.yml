server:
  servlet:
    session:
      timeout: 1h

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/license_server
    username: byport
    password: Vq2$CIhw1C0}W42
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

spring doc:
  api-docs:
    path: /api-docs

logging.level.org.springframework.security: DEBUG
